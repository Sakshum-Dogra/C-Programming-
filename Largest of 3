#include <stdio.h>

int main()
{
  int A, B, C;
  printf("Enter 1st no: ");
  scanf("%d", &A);
  printf("Enter 2nd No: ");
  scanf("%d", &B);
  printf("Enter 3rd no: ");
  scanf("%d", &C);
  if (A >= B)
  {
  if (A >= C)
  {
  printf("A is the greatest");
  }
  else
  {
  printf("C is the greatest");
  }
  }
  else
  {
  if (B >= C) {
  printf("B is the greatest");
  }
  else
  {
  printf("C is the greatest");
  }
  }

  return 0;
}
