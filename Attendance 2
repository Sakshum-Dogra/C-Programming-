#include <stdio.h>
int main()
{
  int A;
  int M;
  printf("Enter Attendance");
  scanf("%d",&A);
  printf("Enterr Marks");
  scanf("%d",&M);
  if (A>75 && M>50)
  {
  printf("Eligible");

  }
  else
  {
  printf("Not Eligible");
  }
  return 0;
}
